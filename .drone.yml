kind: pipeline
name: default

steps:
- name: build
  image: debian
  settings:
    tags: latest
    username:
      from_secret: username
  commands:
    - cat ${username}
---
kind: secret
name: username
get:
  path: secret1
  name: message
