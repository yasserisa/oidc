kind: pipeline
name: master

steps:
- name: build
  image: debian
  settings:
    tags: latest
    username:
      from_secret: username
  commands:
    - cat ${username}

kind: secret
name: username
get:
  path: secret
  name: message
