kind: pipeline
name: build

platform:
  os: linux
  arch: amd64

steps:
- name: build
  image: plugins/docker
  environment:
    TOKEN:
      from_secret: TOKEN
  settings:
    username: gitlab-ci-token
    password: $TOKEN
    repo: yasser.isam/oidc
    registry: registry.gitlab.com
