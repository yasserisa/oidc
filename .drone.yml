kind: pipeline
name: default

steps:
- name: build
  image: debian
  settings:
    tags: latest
  enviroment:
    VALUE:
      from_secret: value
  commands:
    - cat ${VALUE}
---
kind: secret
name: value
get:
  path: secret/message
  name: value
